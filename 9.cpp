#include <bits/stdc++.h>
using namespace std;

int main() {

    // compilation error - when there is some syntax error
    // memory limitation error - code execution if takes more than the limited space, this error is on online coding platforms
    // runtime error or segmentation fault - when illegal acess of memory is tried, like you are trying to access a[n] in array of length n, or like accessing a pointer which is not initialised, also 4 / 0 is a runtime error this is something which is undefined behaviour
    // time limit exceeded - in contest this time limit given is of execution time

    // in recursion, it creates auxiliary space, both error can come in this runtime error or time limit exceeded error


    return 0;
}